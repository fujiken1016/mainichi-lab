# AdSense審査に向けたポータル強化（2026-08-13）

対象：`mainichi-lab.com`（ポータル本体のみ。サブドメイン側リポジトリは未変更）

AdSenseは申込時にサブドメイン単体を受け付けず、ルートドメイン `mainichi-lab.com` での登録になるため、
**審査員が最初に見るのはこのポータルのトップページ**。それまでの実態は「カード5枚＋法的ページ4種」だけで、
Googleの言う「価値の低いコンテンツ／独自コンテンツの不足」で落ちる典型的な形だったため、
読み物として成立するサイトへ作り直した。

---

## 1. 審査コード（パブリッシャーID）

- ID：`ca-pub-8289616283786904`
- 設置：**全11ページの `<head>` 内**（index / story / read/index / 記事3本 / about / privacy / disclaimer / contact / 404）
- スニペット：`<script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-8289616283786904" crossorigin="anonymous"></script>`
- `ads.txt` は**未設置**（審査通過後の作業）。
- 審査リクエストは**押していない**（フジケン本人が実施）。

---

## 2. 追加・変更したコンテンツ

### 新規ページ（5枚）

| URL | 内容 |
|---|---|
| `/story.html` | 「毎日ラボとは」＝運営方針。作りはじめた理由、作るときに決めている4原則、収益（広告）の考え方、更新方針。運営者の顔が見える構成 |
| `/read/` | 読みもの一覧（記事ハブ） |
| `/read/keep-going-system.html` | 独学が続かない原因を「再開コスト／単位の大きさ／選ばせている／記録がない」の4点に分解。宅建GYM・雀トレの実装判断を実例として使用 |
| `/read/lessons-building-apps.html` | 実際に出したUIの失敗5件（非同期と共有DOMの事故で正しい手が不正解になったバグ、速すぎる表示、40px未満のタップ標的、連打・二重送信、ハッピーパスしかテストしない）と、そこから決めたルール |
| `/read/no-login-design.html` | ログイン不要・端末内保存を選んだ理由と、その代償（機種変更で消える／端末をまたげない／サポート不可）を正直に書いた設計判断の記事 |

記事は各2,300〜2,800字（空白除く）。すべて実体験ベースで、実績・ユーザー数・合格実績などの数値は**一切書いていない**。

### トップページ（`/index.html`）全面改稿

- 一行説明のカード → **各サービス400〜600字の紹介文**（何ができるか／特徴／こんな人向け／機能リスト／CTAボタン）
- 冒頭にヒーロー文（なぜこのサイトがあるのか）
- 読みもの3本への導線セクション
- グローバルナビ（ホーム／毎日ラボとは／読みもの／運営者情報／お問い合わせ）をヘッダーに追加、フッターは7リンクに拡張
- JSON-LD（WebSite + Person publisher）を追加

### 記述を実態に合わせた修正

- 麻雀ツールのカード名を **「麻雀のいろは」→「雀トレ（麻雀トレーナー）」** に変更（本番サイトのタイトルが `雀トレ` のため。名称不一致は審査でマイナス）
- 各サービスの説明は本番サイトを実際に読んで書き起こし。件数（方言数・問題数・語彙数）は**ポータル側に数値を転記せず**「サイト内に実数で表示」と書くに留めた（数値のズレ＝虚偽リスクの回避）

---

## 3. 法的ページ・広告配信の記載

### 致命的だった点：法的4ページが全部 `noindex` だった

前回コミット（`8b7ab36`）で非公開ファイルを noindex にした際、
**privacy / about / contact / disclaimer の4ページにも `<meta name="robots" content="noindex">` が入っていた**。
AdSenseはプライバシーポリシーの存在を確認するため、これは審査の直接的なリスク。→ **4ページとも noindex を削除**（404.htmlのみ `noindex,follow` を維持＝正しい設定）。

### `/privacy.html` の広告配信記載を強化

既存の記載に加えて、AdSenseが求める要素を明示：

- 第三者配信事業者がCookieを使用して過去のアクセス情報に基づき広告を配信すること
- Googleが広告Cookieを使用することでGoogleおよびパートナーが広告を表示できること
- ユーザーが[広告設定](https://adssettings.google.com/)でパーソナライズ広告を無効化できること／aboutads.info への導線
- [Googleの広告に関するポリシー](https://policies.google.com/technologies/ads?hl=ja)へのリンク
- アフィリエイト（A8.net／バリューコマース／もしも／Amazonアソシエイト）とPR表記の方針
- 「2. 利用目的」「7. お問い合わせ（削除依頼窓口）」を新設

### その他の法的ページ

- `/about.html`：運営サービス5件の一覧、コンテンツ制作方針（生成AIは補助、責任は運営者）、収益についてを追記。運営者表記は **「フジケン（個人事業主）」** で方言ラボ・宅建GYMと統一済み
- `/contact.html`：受け付けている内容（不具合／要望／訂正・削除依頼／取材）、問い合わせ時に添えてほしい情報、個人情報の取り扱いを追記
- `/disclaimer.html`：ゲーム関連ツール（麻雀・ポーカーは賭博ではなく現金を扱わない旨）、データ保存（端末内保存・復旧不可）、著作権の3節を追加

### アフィリエイトリンク

**ポータル側には1本も存在しない**（新規作成もしていない）。
トレ飯相棒（別リポジトリ）にはPR表記つきのアフィリエイト枠があることを確認済み。`rel` 属性の点検は別リポジトリ側の作業。

---

## 4. 技術面

- `/sitemap.xml` 新規作成（10URL）
- `/robots.txt` 新規作成（`Allow: /` ＋ Sitemap宣言）。これまでCloudflareのマネージドrobots.txt（content signalsのみ・Sitemap宣言なし）が返っていた
- `/favicon.svg` 新規作成（これまで404）
- 全ページに canonical / description / OGP（og:site_name, title, description, type, url）/ twitter:card を設定
- パンくず（`.crumb`）を全下層ページに追加
- CSSを拡張（`.hero` `.svc` `.post-list` `.prose` `.gnav` `.crumb` `.btn`）
- `.claude/` を `.gitignore` に追加（ローカルプレビュー用 launch.json）

### 375px実測（ローカル・Chrome 375×812）

| 項目 | 結果 |
|---|---|
| 横スクロール | **なし**（`documentElement.scrollWidth === 375`） |
| ビューポート超過要素 | **0件** |
| 40px未満のタップ標的 | 本文中のインラインリンクのみ（許容）。ロゴ・CTAリンクは44px以上に修正済み |
| CTAボタン高さ | 49px |
| 記事本文フォント | 16px（当初14.9px→可読性のため引き上げ） |

---

## 5. AdSense審査に向けた現状評価

### 改善したこと（審査で効くはず）

- **独自コンテンツの実体**が0→記事3本＋運営方針1本＋サービス解説5本（合計 約1.5万字）。すべて一次情報（自分の失敗談・設計判断）で、他所からの引き写しがない
- **運営者の顔**が見える（story.html＋about.html＋制作方針＋収益の考え方）
- **ナビゲーション**が全ページ共通（ヘッダー5・フッター7・パンくず・記事間の相互リンク）
- **法的ページが indexable** になった（最大のリスクを除去）
- **プライバシーポリシーにAdSense要件の記述**が揃った
- モバイル対応（375px実測）

### 残リスク

1. **サイト全体のボリュームはまだ小さい**（ポータル本体で11ページ）。「5〜15本の質の高い記事」という一般的な目安に対して読み物は3本。落ちた場合、まず記事を追加するのが最短。ネタは方言ラボ・宅建GYM側に既にあるので、ポータルからそちらへ送るだけでなく**ポータル自身の記事を月1〜2本**足していきたい
2. **サブドメイン側の品質は審査に引きずられる可能性**。ルートドメイン登録なので、審査員がサブドメインまで見に行く可能性は否定できない。ポケチップ（chip）は**meta descriptionが無く、フッター表記が「まいにちラボ」で表記ゆれ**している。ポータルとは別リポジトリなので今回は未修正
3. **ポーカー・麻雀ツールの誤解リスク**。AdSenseはギャンブル関連に厳しい。ポケチップは現金を扱わないが、審査員が誤認する余地はある。免責事項に「現金その他の財産上の利益のやり取りを扱う機能はなく、賭博行為を助長する意図もない」と明記して対処したが、**もし不承認になった場合の最有力容疑**はここだと見ている
4. **新規ドメインで被リンク・検索流入がほぼ無い**。審査そのものの必須条件ではないが、Search Console でのインデックス登録は済ませておいたほうがよい（sitemap.xml を今回追加したので送信可能）
5. **GA4とAdSenseのタグが両方入っている**。問題はないが、AdSenseの自動広告をONにすると審査前でも広告枠プレースホルダが出ることがある。ONにするのは承認後で構わない

### 要判断（フジケン側）

- **審査リクエストを押すタイミング**：この状態で出すか、読み物をあと2〜3本足してから出すか。落ちると再申請までの間隔が空くので、個人的には**あと2本足してから**を推す
- Search Console への sitemap 送信（`https://mainichi-lab.com/sitemap.xml`）
- ポケチップの表記ゆれ（まいにちラボ／毎日ラボ）と meta description 欠落を chip 側リポジトリで直すか
